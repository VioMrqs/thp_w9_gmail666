<%= link_to "Recevoir un email", emails_path, :method => :post, remote: 'true', class: "btn", value: "sound", onclick: "playMusic()"%>

<div class="boxes">
  <div class="list_email">
    <h1>Boîte mails</h1>
      <ol id="email-list">
        <% @emails.each do |email| %>
          <% if email.read %>
            <div class="email-elt" >
              <li><%= link_to email.object, email_path(email), remote: 'true' %></li>
            </div>
          <% else %>
            <div class="email-elt">
              <li><%= link_to email.object, email_path(email), remote: 'true' %></li>
            </div>
          <% end %>
        <% end %>
      </ol>
  </div>

  <div class="selected_content">
    <h1 id="selected_email">E-mail</h1>
      <div id="selected_content"></div>
  </div>
</div>

<script>
function playMusic(){
  console.log('<%= asset_path('/audios/evil-intense-synth_C.wav') %>')
  var music = new Audio('https://www.shockwave-sound.com/sound-effects/halloween-sounds/Evil%20Witch%20Laughing.mp3');
  music.currentTime = 0;
  music.play();
  }
  </script>



